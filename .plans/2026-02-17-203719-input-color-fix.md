# Change Inputs color from amber to teal, match result label shades

**Date:** 2026-02-17 20:37
**Task:** Amber/yellow evokes warning/problem. Switch to a neutral color like teal/emerald. Make result grid labels use same shade as section titles.

## Approach
- Replace `amber` accent with `teal` — calm, neutral green-blue
- Result grid label classes should use `accentStyles.*.title` (same as section headers) instead of `accentStyles.*.label`

## Files to Modify
- `apps/web/src/app/page.tsx`
