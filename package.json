{
  "name": "prompttester",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "prepare": "git config core.hooksPath .githooks && git lfs install",
    "build": "npm run build --workspaces --if-present",
    "lint": "eslint --max-warnings 0 .",
    "typecheck": "tsc --noEmit -p apps/web/tsconfig.json && tsc --noEmit -p apps/landing/tsconfig.json && tsc --noEmit -p packages/types/tsconfig.json"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "eslint-import-resolver-typescript": "^4.4.4",
    "eslint-plugin-boundaries": "^5.0.0",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.0.0",
    "zod-to-json-schema": "^3.25.1"
  }
}
