# Push commit and start dev server

**Date:** 2026-02-17 17:39
**Task:** Configure Railway build/start commands, push commit to trigger deploy, start local dev server

## Approach
1. Set build command (`npm install && npm run build`) and start command (`npm start`) on Railway via variables or API
2. Push current state to main to trigger Railway auto-deploy
3. Start local dev server with `npm run dev`
4. Provide both URLs
